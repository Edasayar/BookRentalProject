@model BorrowBookRequestDto
@{
    ViewData["Title"] = "BorrowBook";
    Layout = "~/Views/AdminLayout/_AdminLayout.cshtml";
}

@* <form asp-action="BorrowBook">
    <div class="form-group">
        <label asp-for="Member">Üye Adı</label>
        <input asp-for="Member.FirstName" class="form-control" />
        <span asp-validation-for="Member.FirstName" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Book.Title">Kitap Adı</label>
        <input asp-for="Book.Title" class="form-control" />
        <span asp-validation-for="Book.Title" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Ödünç Al</button>
</form> *@

